const mysql = require('mysql2');

const Usersgroup = function(groupe) {
    this.id = usersgroup.id;
    this.users_id = usersgroup.users_id;
    this.groupes_id = usersgroup.groupes_id;
}

module.exports = Usersgroup;